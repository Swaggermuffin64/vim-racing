{
  "name": "vim-racing",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "dev": "tsx watch index.ts",
    "build": "tsc && cp codeSnippets.json dist/",
    "generate-snippets": "tsx ./scripts/generateSnippets.ts",
    "start": "node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "hathora": "export $(cat .env | xargs) && ./bin/hathora",
    "hathora:log": "export $(cat .env | xargs) && ./bin/hathora log",
    "hathora:deploy": "export $(cat .env | xargs) && ./bin/hathora deploy --container-port 7777 --transport-type tcp --rooms-per-process 50 --from-latest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "@fastify/rate-limit": "^10.3.0",
    "@hathora/cloud-sdk-typescript": "^2.6.0",
    "fastify": "^5.6.1",
    "jsonwebtoken": "^9.0.3",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^24.7.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
